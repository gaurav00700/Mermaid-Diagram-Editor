FROM mcr.microsoft.com/playwright/python:v1.60.0-jammy

WORKDIR /app
COPY pyproject.toml uv.lock export_options.json README.md ./
COPY src/ ./src/
RUN pip install uv \
    && uv python install 3.12 \
    && uv sync --frozen --no-dev --python 3.12
ENTRYPOINT ["uv", "run", "mermaid-diagram-mcp"]
